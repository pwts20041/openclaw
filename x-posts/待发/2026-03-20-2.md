<!-- angle: 翻车反转 | score: 7 | image: x-posts/images/2026-03-20-2.png -->

sudo -u alice 启动服务，端口冲突启动失败。

明明切了用户啊？反复检查进程、端口、配置文件，全没问题。排查了快一小时。

真相：sudo 默认只切用户身份，不切 HOME 环境变量。程序读的还是你自己的 config，端口当然冲突。

加个 -H 就好了：`sudo -H -u alice`，HOME 才会跟着切。

同一台机器跑多实例的人迟早踩这个坑。记住：sudo 切人不切家。
