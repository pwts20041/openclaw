# 2026-03-20

## Feishu 群聊 mention 兜底规则（@ALL 只给 main）

- 用户确认“现在的组织就是一个人，6 个 agent”，希望群里 `@ALL` 能当作“喊 AI”使用。
- 决策：**只让 main（小虾）响应 `@All/@ALL`**，其余 5 个 agent 不吃 `@ALL`，避免多 agent 抢答/刷屏。
- 已落地到配置：`~/.openclaw/openclaw.json` → `agents.list[main].groupChat.mentionPatterns` 增加 `@All`、`@ALL`，并触发重启生效。

## Ada（openclaw-team 实例）本次排查结论（待继续）

- 记忆回查：Ada 是独立实例（openclaw-team 用户/实例），历史收敛规则：**loopback 端口 18790**（主实例 18789），并且重点检查 3 件事：端口/Feishu bot 身份/workspace。
  - 参考：`memory/2026-03-19.md` 里已沉淀：18790、`cli_a905820a9478dcc9`、workspace `/Users/openclaw-team/workspace`。
- 本次（2026-03-20 晚）在主实例侧实测：`curl http://127.0.0.1:18790/` 连接失败 ⇒ **Ada gateway 当前不可达/未监听**。
- 主实例当前 `agents.list` 中不包含 Ada（因此主实例里“看不到 Ada session/agent”，需要到 openclaw-team 实例去查）。

## 环境/排障小坑

- 本机工具缺失：`rg`、`lsof`、`netstat`、`ss` 在当前 shell 环境里不可用（command not found）。排查时改用 `grep` + `curl` 为主。

## Ada 修复（openclaw-team）— 2026-03-20 晚
- 现象：`ws://127.0.0.1:18790` / `http://127.0.0.1:18790/` **ECONNREFUSED**，Ada gateway 不在监听。
- 关键点：用 `sudo -n -H -u openclaw-team ...` 才能让 `HOME=/Users/openclaw-team`，否则会误用 `HOME=/Users/geyunfei` 导致 CLI 指向错误 config/端口。
- 处理：直接以前台方式启动（避免 `nohup: can't detach from console`）：
  - `sudo -n -H -u openclaw-team openclaw gateway run --bind loopback --port 18790 --force`
- 结果：18790 恢复监听；Feishu `ws client ready`；拐爷确认“现在好了”。

## Cron/Skill 路径漂移（需要修复，避免定时任务空跑）
- `每日PR/工作汇总` cron：当前引用 `skills/daily-report/SKILL.md`，但 workspace 内该路径不存在（ENOENT）。
- `Random Chain Watchdog (light)` cron（job id: `b0fef63a-9777-46ac-83a3-0d33987f05b0`）：当前引用 `skills/chain-watchdog/SKILL.md`，但 workspace 内该路径不存在（ENOENT）。
- `盐选每日复盘：数据分析+热点研究+Skill优化` cron：提示 `skills/zhiforge/` 目录不存在（404），导致复盘/数据链路无法按原设计执行。

> 后续处理口径：要么补齐 skill 目录，要么把 cron payload 改成指向现存 skill（否则每天空跑烧 token）。
