## OpenClaw PR follow-up (yfge) — 2026-03-17

- User asked to “查下 openclaw 的 PR / 是我发出去的 PR / 处理所有的问题”.
- Confirmed GitHub login for this environment: `yfge`.
- Open PRs by `yfge` in `openclaw/openclaw`:
  - #47969 Feishu: route feishu_chat tool via session accountId (branch `fix/feishu-chat-tool-account-routing`)
  - #46907 fix(cron): tolerate object-shaped delivery.channel (branch `fix/issue-46899`)
  - #45922 fix(status): show gateway reachable when probe is scope-limited (branch `fix/45908-gateway-probe-scope-limited`)
  - #45916 fix(status): treat scope-limited gateway probe as reachable (branch `fix/issue-45908`)
  - #44630 fix(docker): allow whitespace in OPENCLAW_CONFIG_DIR (green)
  - #43757 fix(cron): keep announce delivery targets for cron requester sessions (green)
  - #42988 test(gateway): guard spawnedBy support for agent-scoped subagent/acp sessions (green)
  - #41678 fix(feishu): include interactive card content in inbound messages (green)
  - #39547 fix(gateway): circuit-break outbound duplicate sends (green)

- CI failures snapshot (from `gh pr view --json statusCheckRollup`):
  - #47969 failing: `pnpm test:extensions`, `pnpm check`, `startup-memory`, multiple `checks-windows` shards.
  - #46907 failing: `check-docs`, `pnpm test` (one shard), `pnpm check`, several `checks-windows` shards.
  - #45922 failing: `check-docs`, `pnpm test` (one shard), `pnpm check`, several `checks-windows` shards.
  - #45916 failing: `pnpm check`.

- Root cause evidence (from failed run log for #45916 `gh run view 23121467194 --log-failed`): TypeScript errors in tests due to upstream type changes, e.g.
  - `src/auto-reply/reply/dispatch-from-config.test.ts`: TS2322 (`"handled"` etc not assignable to `"no_handler"`)
  - `src/plugins/conversation-binding.test.ts`: TS2339 union narrowing issue (`binding` not on error variant)

- Local work attempt on #45916:
  - Checked out via `gh pr checkout 45916` → branch `fix/issue-45908`.
  - Attempted `git merge upstream/main` → merge conflict in `src/commands/status.scan.test.ts` (multiple conflict markers).
  - Local `pnpm check` run failed due to unrelated formatting parse error in a non-openclaw workspace path (`ai-video-studio/...useStoryboardActions.ts`), indicating the repo workspace contains many untracked non-repo directories and oxfmt is scanning them; needs cleanup / repo isolation before running `pnpm check` reliably.

- ACP harness note:
  - Tried to spawn ACP session to auto-fix all PRs; ACP runtime backend not configured (`ACP runtime backend is not configured. Install and enable the acpx runtime plugin.`). Also Feishu has no thread bindings, so `mode=session thread=true` not available.

## K8S prod clusters (ai-shifu) — 2026-03-17

User confirmed both are **ai-shifu production environments**:

- **Domestic (CN) prod**
  - kubectl context: `ack-aigc-cn`
  - control plane: `https://39.105.21.157:6443`

- **Overseas (US) prod**
  - kubectl context: `ack-aishifu-us`
  - control plane: `https://47.251.242.70:6443`

## Model alias 验证 — 2026-03-17 14:42

- 用 `session_status` model override 逐个测试了所有 7 个别名，全部通过：
  - `/opus` → `anthropic/claude-opus-4-6` ✅
  - `/gemini` → `google/gemini-2.5-pro` ✅
  - `/gpt` → `openai-codex/gpt-5.3` ✅
  - `/gpt52` → `openai-codex/gpt-5.2` ✅
  - `/codex` → `openai-codex/gpt-5.3-codex` ✅
  - `/gpt54` → `openai-codex/gpt-5.4` ✅
  - `/qwen` → `ollama/qwen3:30b-a3b` ✅
- 之前 "not allowed" 的根因：裸模型名被拼到默认 provider `openai-codex` 上，必须带完整 `provider/model` 或用 alias。

## IDENTITY.md 恢复 — 2026-03-17 14:50

- 磁盘上的 IDENTITY.md 被 git checkout 切分支时覆盖成了默认模板。
- 用 Write 整文件写回自定义版本（小虾身份 + 底线规则），commit `ec09883481`。
- 新增底线规则：**长期记忆写入后立刻 git commit**（`git add -f memory/ && git commit --no-verify`）。
- `.gitignore` 有 `/memory/` 规则，需要 `git add -f`。

## 共享 Agents Roster — 2026-03-17 16:02

- 创建 `/Users/geyunfei/dev/openclaw/shared/agents-roster.md` 作为唯一事实源。
- 各 workspace 建 symlink `AGENTS-ROSTER.md` → 源文件：
  - `workspace/` (main/fortune/project/yunying 共用) ✅
  - `workspace-jaoyan/` (阿月) ✅
- 更新一处，所有 agent 自动生效。

## 新增 Agent — 2026-03-17

### 运营 agent (yunying) — 15:53
- agent id: `yunying`, name: 运营
- 飞书 bot: appId `cli_a933bbd788f81bd4`
- binding: feishu account `yunying` → agent `yunying`
- 加入 agentToAgent allow 白名单
- 审批了用户 `ou_115e8d968590ee7a3e415a757cc6f3bf` 的 pairing (code MCYVK6VX)

### 作家 agent (writer) — 18:14 ✅ 已完成
- agent id: `writer`, name: 作家
- 飞书 bot: appId `cli_a93394bf4539dcc8`
- binding: feishu account `writer` → agent `writer`
- 加入 agentToAgent allow 白名单
- 已更新共享 agents-roster

## Agent 全局架构 — 2026-03-17（最终状态）

### 所有 Agent 列表

| Agent ID | 名字 | Workspace | 飞书 Bot AppId | 状态 |
|---|---|---|---|---|
| `main` | 小虾 🦐 | `~/dev/openclaw/workspace` | `cli_a921ba61c9f85cc9`（默认） | ✅ |
| `jaoyan` | 阿月 🌙 | `~/dev/openclaw/workspace-jaoyan` | `cli_a93cfd7932385cbb` | ✅ |
| `fortune` | 文闻 | `~/dev/openclaw/workspace-fortune` | `cli_a932951213b89cca` | ✅ |
| `project` | Push姐 | `~/dev/openclaw/workspace-project` | `cli_a93291828e78dcb2` | ✅ |
| `yunying` | 小蔓 | `~/dev/openclaw/workspace-yunying` | `cli_a933bbd788f81bd4` | ✅ |
| `writer` | 李闯 | `~/dev/openclaw/workspace-writer` | `cli_a93394bf4539dcc8` | ✅ |
| `mc-gateway-…` | Gateway Agent | `~/dev/openclaw/workspace/workspace-gateway-…` | - | ✅ |

### 不在线的实例（其他 macOS 用户）

| 实例 | macOS 用户 | 说明 |
|---|---|---|
| Ada (`openclaw-team`) | `openclaw-team` | 有公众号发布能力，gateway 未运行。auth-profiles 已同步 openai-codex token。 |
| 小吉 (`liurui`) | `liurui` | gateway 未运行 |

### 关键配置

- **所有 workspace 独立**：每个 agent 有自己的 workspace 目录
- **共享名册**：`/Users/geyunfei/dev/openclaw/shared/agents-roster.md`，各 workspace 通过 symlink `AGENTS-ROSTER.md` 引用
- **Agent-to-Agent 白名单**：`main`, `jaoyan`, `fortune`, `project`, `yunying`, `writer`
- **Bindings**：每个飞书 account → 对应 agent（jaoyan/fortune/project/yunying/writer）
- **ownerAllowFrom**：`ou_9f2643f721b113274c9222a4c257ffbb`（拐爷）+ 3 个其他 ou_id

### K8S 生产集群

- **国内（CN）**：kubectl context `ack-aigc-cn`，control plane `https://39.105.21.157:6443`
- **海外（US）**：kubectl context `ack-aishifu-us`，control plane `https://47.251.242.70:6443`
- kubeconfig: `~/.kube/config`

### 模型配置

- **默认模型**：`openai-codex/gpt-5.2`
- **Fallbacks**：`anthropic/claude-sonnet-4-6` → `openai-codex/gpt-5.3` → `google/gemini-2.5-pro` → `ollama/qwen3:30b-a3b`
- **Aliases**：opus / gemini / gpt / gpt52 / codex / gpt54 / qwen
- **OpenAI Codex 认证**：OAuth 模式，refresh token 有时效。过期需手动 `openclaw doctor` 重新登录。
- **Ada 的 openai-codex token**：已于 2026-03-17 19:13 从 main 同步过去，有效期到 3月27日。

### 博客目录

- `~/dev/github-repos/yfge.github.com/`（Jekyll，文章在 `_posts/`）

### 操作备忘

- `sudo -u openclaw-team` 可免密访问 Ada 的文件
- Ada 的 auth-profiles：`/Users/openclaw-team/.openclaw/agents/main/agent/auth-profiles.json`
- `.gitignore` 有 `/memory/` 规则，git add 需要 `-f`

## 教训记录 — 2026-03-17

拐爷指出这两天操作质量差，总结的改进点：
1. 切分支前先 `git stash` 保护 workspace 自定义文件
2. 工具调用失败一次就换方案，不重复试
3. Edit 之前先 `cat` 确认磁盘实际内容
4. memory 写完立刻 `git add -f && git commit --no-verify`
