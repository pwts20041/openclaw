<Page
    x:Class="OpenClawWindows.Presentation.Settings.InstancesSettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <!-- Mirrors InstancesSettings.swift: presence beacon cards with host, platform, mode, age. -->
    <Grid Padding="20,16,20,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,4">
            <StackPanel Spacing="2">
                <TextBlock Text="Connected Instances" Style="{StaticResource BodyStrongTextBlockStyle}" />
                <TextBlock
                    Text="Latest presence beacons from OpenClaw nodes. Updated periodically."
                    FontSize="12"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>
            <Button
                Content="Refresh"
                Command="{Binding RefreshCommand}"
                HorizontalAlignment="Right"
                VerticalAlignment="Top" />
        </Grid>

        <TextBlock
            Grid.Row="1"
            Text="{Binding StatusMessage}"
            FontSize="12"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            Margin="0,0,0,8"
            Visibility="{Binding StatusMessage, Converter={StaticResource StringToVisibilityConverter}}" />

        <ListView
            Grid.Row="2"
            ItemsSource="{Binding Instances}"
            SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="0,6,0,6" ColumnSpacing="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Spacing="2">
                            <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" />
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <TextBlock
                                    Text="{Binding Platform}"
                                    FontSize="11"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                <TextBlock
                                    Text="{Binding Mode}"
                                    FontSize="11"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            </StackPanel>
                            <TextBlock
                                Text="{Binding LastInputDescription}"
                                FontSize="11"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>

                        <!-- Presence dot + age — mirrors presenceIndicator(_:) in InstancesSettings.swift -->
                        <StackPanel Grid.Column="1" Spacing="4" VerticalAlignment="Center" HorizontalAlignment="Right">
                            <Ellipse
                                Width="8"
                                Height="8"
                                HorizontalAlignment="Center"
                                Fill="{Binding IsActive, Converter={StaticResource BoolToGreenRedBrushConverter}}" />
                            <TextBlock
                                Text="{Binding AgeDescription}"
                                FontSize="10"
                                HorizontalAlignment="Center"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
